import 'package:flutter/material.dart';

class ContinueCourseCard extends StatelessWidget {
  const ContinueCourseCard({super.key});

  @override
  Widget build(BuildContext context) {
    return const Placeholder();
  }
}
