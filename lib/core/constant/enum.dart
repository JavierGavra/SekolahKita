enum CourseType { reading, writing, numeration }

enum ModuleType { lesson, quiz }

enum ModulStatus { locked, available, completed }

enum LessonSectionType { text, audio, drawLetter, multipleChooice }

enum QuizQuestionType {
  speechRecognition,
  multipleChooice,
  listening,
  multipleSound,
  writingTrace,
}
